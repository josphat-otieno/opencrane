/** Supported surfaces for the shared expandable section. */
export enum CollapsibleSectionVariants
{
	/** Light content panel with a dividing rule. */
	Panel = "panel",
	/** Compact treatment for a dark navigation rail. */
	Rail = "rail"
}
