import { ___CreateLogger, type Logger } from "@opencrane/backend/observability";

/** Process-wide structured logger for the channel trust boundary. */
export const _log: Logger = ___CreateLogger("channel-proxy");
