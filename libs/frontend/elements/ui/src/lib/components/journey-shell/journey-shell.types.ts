/** Finite content widths supported by the shared journey shell. */
export enum JourneyShellLayouts
{
	/** Focused entry or single-question journey. */
	Compact = "compact",
	/** Result or conversation composition that needs more horizontal room. */
	Wide = "wide"
}
