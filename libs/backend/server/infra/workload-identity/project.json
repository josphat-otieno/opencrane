{
  "name": "backend-server-infra-workload-identity",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/backend/server/infra/workload-identity/src",
  "tags": ["type:lib", "layer:infra", "scope:workload-identity"],
  "targets": {
    "lint": { "executor": "nx:run-commands", "options": { "command": "tsc -p tsconfig.json --noEmit", "cwd": "libs/backend/server/infra/workload-identity" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "vitest run", "cwd": "libs/backend/server/infra/workload-identity" } }
  }
}
