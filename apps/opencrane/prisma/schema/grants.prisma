enum GrantScope {
  Org        @map("org")
  Department @map("department")
  Team       @map("team")
  Project    @map("project")
  Personal   @map("personal")
}

enum GrantSubjectType {
  Group  @map("group")
  User   @map("user")
}
