# Keep the Docker build context lean for every image built from the repo root.
# The operator Dockerfile COPYs `libs` wholesale, so installs/builds must never
# leak host node_modules or stale dist output into the image.
**/node_modules
**/dist
.git
.claude
.nx
**/.angular
website/.vitepress/cache
