{{- if and .Values.multiCt.enabled (not .Values.networkPolicy.mainNetworkDefaultDeny.enabled) }}
{{- fail "multiCt.enabled=true requires networkPolicy.mainNetworkDefaultDeny.enabled=true: the platform-namespace default-deny (OpenCrane, channel-proxy, LiteLLM, Cognee, and Obot) is mandatory under a multi-ClusterTenant install — a shared platform namespace serving many ClusterTenants must never stay open by default. Set networkPolicy.mainNetworkDefaultDeny.enabled=true (after validating plane-to-plane connectivity per the values.yaml note), or drop the --multi-ct profile for a single-tenant box." }}
{{- end }}
{{- if and .Values.networkPolicy.enabled (or .Values.networkPolicy.mainNetworkDefaultDeny.enabled .Values.multiCt.enabled) }}
{{- /*
  Platform-namespace default-deny (values.yaml networkPolicy.mainNetworkDefaultDeny, #151
  item 3 / scout gap (c)).

  CONTEXT: the operator emits a per-silo default-deny into each ClusterTenant's own namespace
  at runtime (_BuildSiloBaselineNetworkPolicy). This template is the missing counterpart for
  the PLATFORM namespace itself — where OpenCrane, channel-proxy, LiteLLM, Cognee, and Obot
  run — which had no default-deny before this change.

  RELATION TO OTHER POLICIES: networkpolicy-planes.yaml + the app-owned OpenCrane ingress template
  remain the precise, per-port allow-lists; this template is the coarse belt-and-suspenders
  floor underneath them. networkpolicy-multi-instance.yaml is the CROSS-instance/cross-silo
  floor (a different namespace boundary); this one is INTRA-namespace.

  GATE: `enabled` defaults to FALSE (see values.yaml for the staged validation rollout this
  flag expects before flipping it on in production). MANDATORY (see the `fail` guard above)
  once `multiCt.enabled=true`.

  FQDN-BASED EGRESS: rule 7 allows port 443 to ANY external destination (conservative — a
  NetworkPolicy cannot match on FQDN). Replace with CiliumNetworkPolicy scoped to
  networkPolicy.defaultAllowedDomains / your Zitadel host if the cluster runs Cilium.
*/}}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "opencrane.fullname" . }}-platform-default-deny
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "opencrane.labels" . | nindent 4 }}
    app.kubernetes.io/component: platform-default-deny
spec:
  # ArtifactStore, agent-controller, and the PostgreSQL pooler have app-owned policies; each
  # agent-runtime Job has a controller-built policy. Excluding them prevents this generic
  # allow-list from widening their stricter boundaries. CNPG identifies generated pooler pods
  # with cnpg.io/poolerName rather than preserving the Pooler CR's application labels.
  podSelector:
    matchExpressions:
      - key: app.kubernetes.io/component
        operator: NotIn
        values: [artifact-service, agent-controller, agent-runtime, cognee, memory-gateway]
      - key: cnpg.io/poolerName
        operator: DoesNotExist
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # 1. Platform pod ↔ platform pod: the runtime planes call each other constantly
    #    (OpenCrane/channel-proxy -> target planes, Cognee -> LiteLLM, etc.);
    #    re-allow all of that under the default-deny rather than re-deriving every pairwise
    #    port (networkpolicy-planes.yaml already enforces the precise per-port allow-lists).
    - from:
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: opencrane-server
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: litellm
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: cognee
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: mcp-gateway
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: channel-proxy
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: artifact-service
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: opencrane-ui-spa
    # 2. Tenant pods (a different namespace's watch target, but may share this namespace in
    #    single-namespace installs) -> opencrane-ui, INTERNAL port only. Identity is enforced
    #    by TokenReview inside the handler (see apps/opencrane/helm/templates/_ingress.tpl); this rule
    #    is the coarse backstop so that path survives the default-deny.
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: tenant
      ports:
        - protocol: TCP
          port: {{ .Values.clustertenantManager.service.internalPort }}
    # 3. Cluster ingress controller -> opencrane-ui, PUBLIC port (external API traffic).
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.networkPolicy.ingressNamespace | default "ingress-nginx" }}
      ports:
        - protocol: TCP
          port: {{ .Values.clustertenantManager.service.port }}
  egress:
    # 4. Reciprocal platform pod -> platform pod (see ingress rule 1).
    - to:
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: opencrane-server
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: litellm
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: cognee
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: mcp-gateway
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: channel-proxy
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: artifact-service
        - podSelector:
            matchLabels:
              {{- include "opencrane.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: opencrane-ui-spa
    # 5. Database consumers -> CNPG-managed PgBouncer. The Pooler chart has a separate
    #    Helm release identity, so it cannot use this umbrella chart's selectorLabels.
    #    Its app-owned ingress policy remains the authority over which consumers are admitted.
    - to:
        - podSelector:
            matchLabels:
              cnpg.io/poolerName: {{ include "opencrane.postgresPoolerName" . }}
      ports:
        - protocol: TCP
          port: 5432
    {{- if .Values.networkPolicy.allowDNS }}
    # 6. DNS resolution (cluster DNS lives outside this namespace).
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    {{- end }}
    # 7. HTTPS to external LLM-provider / IAM APIs (OpenAI, Anthropic, Zitadel, ...).
    - ports:
        - protocol: TCP
          port: 443
{{- end }}
