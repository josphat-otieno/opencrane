{{- /* Service only exists for the release-local (instance-scoped) skill-registry. A
       shared registry lives outside this release, reached via opencrane.skillRegistryUrl. */ -}}
{{- if and .Values.skillRegistry.enabled (ne (include "opencrane.skillRegistryShared" .) "true") }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "opencrane.fullname" . }}-skill-registry
  labels:
    {{- include "opencrane.labels" . | nindent 4 }}
    app.kubernetes.io/component: skill-registry
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.skillRegistry.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opencrane.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: skill-registry
{{- end }}
