/** Stable identifier of an agent service. */
export type AgentServiceId = string;

/** Stable identifier of an immutable agent revision. */
export type AgentRevisionId = string;

/** Stable identifier of an agent run. */
export type AgentRunId = string;

/** Stable identifier of a conversation thread. */
export type ThreadId = string;

/** Stable identifier of an immutable transcript message. */
export type MessageId = string;

/** Stable identifier of a persona profile. */
export type PersonaProfileId = string;

/** Stable identifier of a persona revision. */
export type PersonaRevisionId = string;

/** Stable identifier of a silo. */
export type SiloId = string;

/** Stable identifier of a user. */
export type UserId = string;
